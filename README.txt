->Command in first terminal

"npm install" and "npm start"

->Command in second terminal 

"npm run food-json"

->At the beginning install all the node modules required by entering the command line "npm install"

->now run the app by command "npm start". It runs at localhost:3000

->Open the new terminal and run the json server(api)
with command line "npm run food-json" it runs at locahost:5000

->All the items added in cart can be viewed in "localhost:5000/cart" and will be added in db.json file

->Items can be be deleted in cart page

Roots added :

"/" for home page 
"/cart" for cart page

